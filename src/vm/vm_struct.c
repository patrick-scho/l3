#include "vm.h"

// Struct *struct_create(char *name) {
//   Struct *result = (Struct*)malloc(sizeof(Struct));

//   result->name = name;

//   return result;
// }


// void struct_destroy(Struct *s) {
//   free(s->name);
//   free(s);
// }
